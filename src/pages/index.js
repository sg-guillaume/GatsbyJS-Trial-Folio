import React from "react"

export default () => (
	<div style={{ display: `flex`, flexDirection: `column`, justifyContent: `space-around`,	height: `100vh`, width: `calc(100vw - 2.5rem)`, textAlign: `center`,	backgroundColor: `#fff` }}>
		<h1>Guillaume Stawarz Portfolio</h1>
	</div>
);